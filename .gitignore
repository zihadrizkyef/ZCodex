node_modules/
dist/
out/
release/
*.log
*.tsbuildinfo

# Protocol bindings are generated from the installed Codex CLI (npm run gen:protocol).
packages/codex-protocol/src/generated/

# Reference checkout of the upstream OpenAI Codex repo (~700 MB, has its own .git).
# Read-only reference: never vendored into this repo.
codex/
